export { MemoryMap } from './memory-map'
export {
  WorkersStateStorage, WorkersStateStorageOptions,
  INVALID_WORKER_INDEX
} from './workers-state-storage'
export {
  STATE_STORAGE_LENGTH, START_OFFSET, MAX_WRITING_WORKERS,
  MAX_READING_WORKERS, WRITE_WAIT_INTERVAL
} from '@/config/workers-state-storage'
